Windows DPI settings
check mac
clip validation tracker
terminate calculations
multiselect listview
graph for kinetic roll energy

build jar file

support saving loading importing exporting

exit, minimize button
resize

add real brachistochrone curve

use raw trace to estimate computation time, using progressbar (primary / secondary)


colormap default 8 colors (represented by byte % 8)
setColor(map.get...)


optimize change listeners
check for infinite loops interpolation (QUICK)